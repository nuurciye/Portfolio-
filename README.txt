
Mahad Portfolio Android App (HTML / PWA)

How to convert to Android APK:

1. Install Node.js
2. Run:

npm init -y
npm install @capacitor/core @capacitor/cli
npx cap init
npm install @capacitor/android
npx cap add android
npx cap sync
npx cap open android

3. Build APK in Android Studio.
